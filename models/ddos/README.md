# ddos

Status: **no ONNX model**. Rules/statistics only: per-destination rate buckets, EWMA baseline, source-cardinality sketch (flink/sih_detect/ddos.py). A bounded GBDT challenger needs credible labelled flood captures first.

This directory intentionally holds no model files. Adding one requires a trained, evaluated bundle written by model-training (see models/dga/README.md) and a deployment-manifest entry.
