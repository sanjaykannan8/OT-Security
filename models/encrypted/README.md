# encrypted

Status: **no ONNX model**. Rules/statistics only: weighted TLS handshake metadata indicators with explicit abstention (flink/sih_detect/encrypted.py). No learned model: no labelled current TLS/QUIC malware captures are available.

This directory intentionally holds no model files. Adding one requires a trained, evaluated bundle written by model-training (see models/dga/README.md) and a deployment-manifest entry.
