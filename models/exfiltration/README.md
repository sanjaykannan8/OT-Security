# exfiltration

Status: **no ONNX model**. Rules/statistics only: sustained outbound volume vs per-source baseline, destination rarity, directional ratio when observable (flink/sih_detect/exfil.py).

This directory intentionally holds no model files. Adding one requires a trained, evaluated bundle written by model-training (see models/dga/README.md) and a deployment-manifest entry.
