"""Deterministic, file-based scenario generation. Nothing here transmits packets: PCAPs and Zeek-format
logs are written to local files only, with ground truth in sidecar manifests (never model inputs)."""
